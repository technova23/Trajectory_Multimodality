# 2026-05-21 worklog

## Nominal-path constrained reach consolidation

- Consolidated the 20k balanced-checkpoint constrained-reach starter work for commit:
  - nominal-path `AvoidRegion` helpers and JSON loader in `pg3d.eval`;
  - `scripts/build_nominal_path_constraints.py` for fixed base-success subset generation;
  - `scripts/eval_constrained_reach.py` support for `--constraints-dir` and
    `--episode-indices-file`;
  - focused tests for nominal-path placement, constraint loading, parser defaults, and
    direct-path backward compatibility.
- Updated durable docs before finalization:
  - `docs/status.md` now records the 7/25 held-out base-success gate as the active blocker;
  - `docs/runbooks/commands.md` includes the 20k base gate, nominal-path builder, and constrained
    eval commands;
  - `docs/adr/0009-nominal-path-constrained-reach-eval.md` records the base-success
    nominal-path eval protocol;
  - `docs/milestones.md` notes the fixed-subset workflow under M7.
- Preserved the existing behavior for historical P10/direct-path commands when precomputed
  constraints are not supplied.

Validation:

- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run pytest`
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run ruff check .`
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make smoke`
- `git diff --check`

Outcome:

- The implementation and docs are ready to commit and push.
- The main constrained reach eval remains scientifically blocked until base reach is stronger than
  the first 20k checkpoint gate result of 7 successes in 25 held-out balanced validation episodes.
