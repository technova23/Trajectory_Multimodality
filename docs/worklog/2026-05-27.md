# Worklog 2026-05-27

## Reach dataset planner retry cleanup

- Suppressed expected ManiSkill `move_to_pose_with_screw` stdout/stderr during dataset-writer
  retry loops by default, with `--show-planner-output` available for planner debugging.
- Changed multimodal reach collection to skip incomplete seed/start groups by default, so a seed is
  not written when any requested trajectory family such as `downward_arc` fails to plan.
- Added `--allow-partial-variant-sets` for explicit compatibility/debug runs that want the old
  partial-group behavior.
- Added focused writer tests for default flags, complete variant-set detection, and planner-output
  suppression.

Validation:

- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run pytest tests/test_maniskill_reach_writer.py`
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run ruff check scripts/write_maniskill_reach_dataset.py tests/test_maniskill_reach_writer.py`

## DP3 Zarr training readiness

- Updated `ReachSequenceDataset` to fit DP3 normalizers from a bounded deterministic subset of
  Zarr timesteps by default instead of reading every stored 1024-point cloud at startup.
- Added `--normalizer-max-steps` to `scripts/train_dp3_reach.py`; `0` keeps the exact full-dataset
  behavior for small/debug runs.
- Kept `target_position` as the preferred goal source for ordered goal tokens and added a
  `goal_pos` fallback for alias-only Zarr files.
- Verified that `artifacts/pg3d_reach_500pairs_4variants.zarr` exposes DP3 shape metadata
  `point_cloud [1024, 3]`, `agent_pos [9]`, and `action [7]`, and that a CPU one-step training
  smoke can start from it.

Validation:

- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/train_dp3_reach.py --dataset artifacts/pg3d_reach_500pairs_4variants.zarr --device cpu --max-steps 1 --batch-size 1 --num-workers 0 --max-train-episodes 1 --val-ratio 0 --normalizer-max-steps 64 --horizon 16 --n-obs-steps 2 --n-action-steps 8 --encoder-output-dim 8 --diffusion-step-embed-dim 16 --down-dims 16 32 --num-inference-steps 1 --no-use-ema --lr-scheduler none --wandb-mode disabled --no-checkpoint-rollout-videos`
