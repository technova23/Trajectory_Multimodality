# 2026-05-19 worklog

## P11 balanced reach reliability

- Added ordered goal-token conditioning for DP3 reach without adding a new policy-visible key:
  `obs.point_cloud` keeps shape `[N, 3]`, and the final K point slots are overwritten from
  `/data/target_position` at dataset-load and live policy-input time.
- Added the simulator-free marker helper with default `goal_marker_points=16` and
  `goal_marker_radius=0.015`; `K=0` remains available for old checkpoint compatibility and
  ablations.
- Extended `DP3Encoder` with a marker MLP branch over the ordered tail points while preserving the
  existing PointNet scene branch over the first `N-K` points.
- Added `PG3DReach-BalancedWorkspace-v0`, a 70/30 mixture of core-practical and bounded-practical
  target regions for the next base reach checkpoint.
- Added `scripts/diagnose_reach_dataset.py` for target-distribution, raw goal-visibility,
  marker-correctness, and train/validation region-balance checks.
- Updated training/eval/rollout/constrained-eval paths so checkpointed marker settings are applied
  consistently in policy input tensors.
- Deferred Franka gripper / custom URDF / Robotiq work to a later manipulation milestone; it is not
  on the critical path for base reach reliability.

Validation so far:

- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run pytest tests/test_dp3_reach_dataset.py tests/test_dp3_policy_smoke.py tests/test_dp3_reach_rollout.py tests/test_constrained_reach_eval.py tests/test_maniskill_reach_writer.py tests/test_dp3_reach_training.py`
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run ruff check pg3d/policies/dp3 pg3d/envs/maniskill_adapter scripts/train_dp3_reach.py scripts/eval_dp3_reach_dataset.py scripts/rollout_dp3_reach_policy.py scripts/eval_constrained_reach.py scripts/compare_world_model_rollout.py scripts/diagnose_reach_dataset.py tests/test_dp3_reach_dataset.py tests/test_dp3_policy_smoke.py tests/test_dp3_reach_rollout.py tests/test_constrained_reach_eval.py tests/test_maniskill_reach_writer.py tests/test_dp3_reach_training.py`
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python -m py_compile scripts/diagnose_reach_dataset.py`
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/diagnose_reach_dataset.py --dataset artifacts/reach-dataset-smoke/pg3d-reach-smoke.zarr --max-rows 5 --goal-marker-points 1`
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run pytest`
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run ruff check .`
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make smoke`
- `git diff --check`

Workstation follow-up:

- The P11 dataset generation and retraining commands were verified on the user's workstation after
  implementation.
- The next scientific gate is to inspect the trained checkpoint's held-out closed-loop metrics and
  videos, then rerun P10 constrained reach only after base reach passes the reliability threshold.

## 20k balanced constrained reach starter

- Selected
  `artifacts/reach-datasets/dp3-reach-balanced-1000-checkpoints/step_00020000.pt` as the starter
  P10 rerun checkpoint, using EMA weights.
- Added `scripts/build_nominal_path_constraints.py` to roll out base DP3 on held-out balanced
  validation episodes, keep only base-success episodes, and write fixed nominal-path
  `AvoidRegion` JSON files plus `episode_indices.txt`, saved TCP paths, and `manifest.json`.
- Added `NominalPathAvoidConfig`, `nominal_path_avoid_region`, and `load_episode_constraints` to
  the pure eval helpers. The default nominal-path sphere is radius `0.03m` at 50% arc length along
  the successful nominal TCP path.
- Extended `scripts/eval_constrained_reach.py` with `--constraints-dir` and
  `--episode-indices-file` so base/rejection/reranking consume the same precomputed constraints
  and dataset episodes. The old direct-path constraint generation remains the default when those
  flags are omitted.
- Recorded the evaluation-protocol decision in
  `docs/adr/0009-nominal-path-constrained-reach-eval.md` and added the 20k starter commands to the
  runbook.

Validation so far:

- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run pytest tests/test_constrained_reach_eval.py`
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python -m py_compile scripts/build_nominal_path_constraints.py scripts/eval_constrained_reach.py`
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run ruff check pg3d/eval scripts/eval_constrained_reach.py scripts/build_nominal_path_constraints.py tests/test_constrained_reach_eval.py`
- `git diff --check`
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run pytest`
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run ruff check .`
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make smoke`
- Tiny real builder smoke:
  `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/build_nominal_path_constraints.py --checkpoint artifacts/reach-datasets/dp3-reach-balanced-1000-checkpoints/step_00020000.pt --checkpoint-model ema --dataset artifacts/reach-datasets/pg3d-reach-balanced-val-100.zarr --episodes 3 --device cuda --max-steps 100 --post-success-steps 0 --min-successes 0 --allow-too-few-successes --output-dir /tmp/pg3d-nominal-path-constraints-smoke-success`
  selected 2 of 3 episodes and wrote two constraint JSON files.
- Tiny precomputed-constraint eval smoke:
  `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/eval_constrained_reach.py --checkpoint artifacts/reach-datasets/dp3-reach-balanced-1000-checkpoints/step_00020000.pt --checkpoint-model ema --dataset artifacts/reach-datasets/pg3d-reach-balanced-val-100.zarr --source dataset --episode-indices-file /tmp/pg3d-nominal-path-constraints-smoke-success/episode_indices_one.txt --constraints-dir /tmp/pg3d-nominal-path-constraints-smoke-success/constraints --methods base rejection reranking --episodes 1 --device cuda --seed 0 --max-steps 2 --post-success-steps 0 --planning-horizon-chunks 1 --execution-horizon-chunks 1 --geometry-mode fast --k-schedule 2 --policy-batch-size 2 --artifact-selection all --output-dir /tmp/pg3d-constrained-precomputed-smoke --allow-failure`
  ran all three methods and recorded `constraint_source.type=precomputed` in `summary.json`.
- Actual 25-episode base gate / constraint-build attempt:
  `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/build_nominal_path_constraints.py --checkpoint artifacts/reach-datasets/dp3-reach-balanced-1000-checkpoints/step_00020000.pt --checkpoint-model ema --dataset artifacts/reach-datasets/pg3d-reach-balanced-val-100.zarr --episodes 25 --device cuda --max-steps 100 --post-success-steps 8 --avoid-radius 0.03 --path-fraction 0.5 --min-successes 15 --output-dir artifacts/reach-datasets/constrained-reach-balanced-20k-nominal-r003-val25`
  selected only 7 base-success episodes out of 25, so it exited nonzero as intended and the main
  constrained eval was not run. Selected dataset episode indices were
  `[1, 2, 5, 10, 14, 15, 22]`, with successful final distances between 1.93 cm and 2.42 cm.

Workstation follow-up:

- Treat base reach as the active blocker for the 20k balanced checkpoint before interpreting
  rejection/reranking. Next useful checks are qualitative videos for the 18 failed held-out
  episodes, longer/continued training, or inference-setting ablations.
