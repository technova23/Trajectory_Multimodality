# Work log — 2026-05-16

## Objective

Prepare Codex-ready project scaffolding and documentation for pg3d.

## Summary

Created starter docs for project objectives, milestones, architecture, ADRs, Codex prompts, runbooks, review checklist, refactor policy, and uv/PyTorch setup.

## Current state

No implementation has started yet. The next real step is to create the private GitHub repository, copy this starter pack into it, create private dependency mirrors, and validate the uv/CUDA environment.

## Next recommended task

Use `docs/prompts/P00_bootstrap_repo.md`, then `docs/prompts/P01_private_mirror_submodules.md`.

## P00 bootstrap scaffold

Implemented the lightweight repo scaffold for the planned architecture:

- added package namespace directories for envs, RLBench adapter, policies, world model,
  constraints, composition, baselines, eval, visualization, logging, and utilities;
- expanded smoke tests/import script to verify those namespaces import without RLBench, PyRep,
  CoppeliaSim, or DP3;
- aligned the uv/PyTorch extra with the documented `cu129` setup path.

No simulator, policy, or submodule logic was added.

Validation:

- `uv sync --extra cu129 --group dev` completed with torch `2.11.0+cu129` and
  `torch.version.cuda == 12.9`;
- `make test`, `make lint`, and `make smoke` passed when run with
  `UV_CACHE_DIR=/tmp/pg3d-uv-cache` because this sandbox cannot write to the default uv cache
  under `~/.cache`;
- `make gpu-check` imported torch `2.11.0+cu129`, but failed because no CUDA device is visible in
  this sandbox.

The next task is still dependency mirroring and workstation GPU validation.

## P02 uv/CUDA and pg3d-native DP3 slice

Implemented the first narrow DP3 policy slice directly under `pg3d/policies/dp3` instead of using
the upstream submodule as the runtime import path. This slice keeps only model-level pieces needed
for synthetic point-cloud policy checks:

- point-cloud/state encoder,
- diffusion action model,
- normalizer and mask generator,
- `SimpleDP3` / `DP3` policy classes,
- synthetic batch/policy helpers.

Added `scripts/smoke_dp3_policy.py` and tests covering:

- importing `pg3d` without importing torch or DP3,
- importing `pg3d.policies.dp3` without simulator dependencies such as Gym, MuJoCo, MetaWorld,
  DexArt, RRL, PyTorch3D, or numba,
- synthetic policy construction,
- synthetic `predict_action`,
- synthetic `compute_loss`, backward pass, and optimizer step.

Dependency/tooling updates:

- added `diffusers` as a pg3d dependency;
- kept PyTorch ownership in the existing `cu129` extra;
- excluded `external/` from Ruff so private submodules are not linted as pg3d source.

Validation notes:

- `uv sync --extra cu129 --group dev --locked` passed;
- `make test` passed with 7 tests;
- `make lint` passed;
- `make smoke` passed and imports `pg3d.policies.dp3`;
- `uv run python scripts/smoke_dp3_policy.py --device cpu` passed;
- user ran `make gpu-check` successfully from their local pg3d terminal on the RTX 5090 workstation
  with torch `2.11.0+cu129`, CUDA `12.9`, device `NVIDIA GeForce RTX 5090`, capability `(12, 0)`,
  and a successful matrix multiply smoke test;
- user ran `uv run python scripts/smoke_dp3_policy.py --device cuda` successfully with action shape
  `(2, 2, 7)`, action prediction shape `(2, 4, 7)`, and finite loss;
- this Codex sandbox still cannot see `/dev/nvidia*`;
- `make gpu-check` still fails in this Codex sandbox with torch `2.11.0+cu129`,
  `torch.version.cuda == 12.9`, and `cuda available: False`.

## P03 RLBench ReachTarget smoke

Added the first RLBench `ReachTarget` smoke path for M1:

- added `scripts/rlbench_smoke_reach.py`;
- kept RLBench/PyRep imports lazy inside the script;
- added an optional `rlbench` uv extra pointing at upstream `stepjam/RLBench` `master`;
- documented CoppeliaSim/PyRep/RLBench setup in `docs/runbooks/rlbench_setup.md`;
- updated command references for RLBench smoke usage.

The smoke script checks for:

- `COPPELIASIM_ROOT`,
- `rlbench`,
- `pyrep`,
- likely `LD_LIBRARY_PATH` / `QT_QPA_PLATFORM_PLUGIN_PATH` issues.

Current Codex environment observation:

- `rlbench` and `pyrep` are not installed;
- `COPPELIASIM_ROOT` is not set;
- the smoke script is expected to produce setup guidance here, not launch CoppeliaSim.
