# Worklog 2026-05-26

## Reach dataset writer viewer mode

- Restored `scripts/write_maniskill_reach_dataset.py` to headless collection by default instead of
  forcing `render_mode="human"`.
- Added explicit `--viewer`, `--viewer-step-delay`, and `--viewer-hold-seconds` flags so live
  collection pumps ManiSkill viewer frames and can keep the window open briefly after collection.
- Documented the viewer command in `docs/runbooks/commands.md` and the black-window cause in
  `docs/runbooks/maniskill_setup.md`.

## Randomized reach starts and markers

- Added a red `start_site` visual marker to the custom ManiSkill reach env, matching the existing
  green `goal_site` marker style.
- Changed the dataset writer default task to `PG3DReach-BalancedWorkspace-v0` for broader table
  scatter in no-arg smoke runs.
- Added planner-validated randomized TCP start sampling before trajectory variant generation, with
  minimum start-goal distance and a `--no-randomize-start` compatibility switch.
- Recorded sampled and actual start positions in episode metadata.

Validation:

- Compileall passed for the reach writer, reach env, and focused writer tests.
- `git diff --check` passed.
- `uv run pytest tests/test_maniskill_reach_writer.py` and `uv run ruff check ...` could not run
  because `uv` is not installed on this PATH.
- Fallback `python3 -m pytest ...` and `python3 -m ruff ...` could not run because pytest and ruff
  are not installed in the system Python.
