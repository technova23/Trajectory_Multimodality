# Work log — 2026-05-17

## Objective

Migrate the active simulator stack from RLBench/PyRep/CoppeliaSim to ManiSkill/SAPIEN without
preserving RLBench as an alternate backend.

## Summary

- Removed active RLBench adapter code, smoke scripts, tests, setup runbook, and generated artifacts.
- Added a lightweight ManiSkill adapter namespace with simulator-neutral observation dataclasses.
- Added a non-rendering ManiSkill `PickCube-v1` smoke script and Makefile target.
- Rewrote current prompts, runbooks, status docs, architecture docs, and simulator ADRs around
  the ManiSkill reach-first path.
- Kept RLBench mentions only as historical migration context in prior worklogs, the superseded
  ADR, the migration ADR, and the status deprecation note.

## Files changed

- `pyproject.toml`, `uv.lock`: removed RLBench/PyRep dependency metadata and added the optional
  `maniskill` extra.
- `pg3d/envs/maniskill_adapter/`: added the minimal ManiSkill adapter namespace and generic
  observation dataclasses.
- `scripts/check_maniskill.py`: added the non-rendering ManiSkill built-in task smoke.
- `scripts/rlbench_smoke_reach.py`, `scripts/rlbench_save_observation.py`: removed.
- `tests/test_maniskill_adapter_types.py`, `tests/test_smoke.py`, `tests/test_dp3_imports.py`:
  updated lightweight tests for simulator-free imports and generic observation typing.
- `tests/test_rlbench_*`: removed.
- `docs/runbooks/maniskill_setup.md`: replaced the RLBench setup runbook.
- `docs/prompts/P03_maniskill_smoke.md`, `docs/prompts/P04_observation_adapter.md`,
  `docs/prompts/P05_reach_dataset_writer.md`: rewrote the active simulator prompts for ManiSkill.
- `docs/adr/0002-maniskill-primary-simulator.md`: recorded the simulator switch.

## Commands run

```bash
UV_CACHE_DIR=/tmp/pg3d-uv-cache uv lock
UV_CACHE_DIR=/tmp/pg3d-uv-cache uv lock --check
UV_CACHE_DIR=/tmp/pg3d-uv-cache uv sync --extra cu129 --extra maniskill --group dev --group notebooks
uv run python scripts/check_gpu.py
uv run python scripts/check_maniskill.py
make smoke
make test
make lint
make gpu-check
make maniskill-check
rg -n -i "rlbench|pyrep|coppelia|coppeliasim|reach_target|reachtarget|rlbench_adapter|rlbench_setup"
```

Outcome:

- Initial sandboxed `uv lock` and `uv sync` attempts hit DNS restrictions; rerunning with approved
  network access succeeded.
- `uv lock --check` passed with 236 packages resolved.
- `uv sync --extra cu129 --extra maniskill --group dev --group notebooks` installed
  `mani-skill==3.0.1` and `sapien==3.0.3`, and removed `rlbench` and `pyrep`.
- `uv run python scripts/check_gpu.py` passed with `torch 2.11.0+cu129`, CUDA 12.9, and RTX 5090.
- `uv run python scripts/check_maniskill.py` passed with `PickCube-v1`, `obs_mode="state"`, and
  one sampled step.
- `make smoke`, `make test`, `make lint`, `make gpu-check`, and `make maniskill-check` passed
  outside the sandbox. The sandboxed `make gpu-check`/`make maniskill-check` variants cannot see
  the GPU/render device.

## Results / observations

- Base `import pg3d` and `pg3d.envs.maniskill_adapter` do not import ManiSkill, SAPIEN, Gymnasium,
  rendering, or GPU libraries.
- `uv.lock` keeps the `cu129` PyTorch packages on the explicit PyTorch CUDA 12.9 index.
- The active docs and prompts now point to ManiSkill/SAPIEN. The modified prompts explicitly ask
  Codex to read `docs/project_proposal.html` immediately after `AGENTS.md`.

## Blockers / risks

- Rendering and point-cloud observation smoke remains intentionally separate; Vulkan/asset handling
  should be validated when the P04 adapter reaches visual observations.
- No full ManiSkill adapter or custom reach task was implemented in this migration.

## Next recommended task

- Run P04: implement the ManiSkill observation adapter for state first, then point-cloud/RGB-D/mask
  observations behind optional rendering checks.

## Review follow-up cleanup

After the migration review:

- pinned the ManiSkill optional dependency to `mani_skill==3.0.1`;
- made P03/P04/P05 explicitly plan-first before edits;
- updated P04 to extend existing observation dataclasses instead of recreating them;
- clarified that `docs/milestones.md` decomposes the source-of-truth proposal into smaller
  Codex-sized execution milestones;
- updated `AGENTS.md`, `docs/prompts/README.md`, `docs/runbooks/maniskill_setup.md`, and
  `README.md` to match the current ManiSkill-first setup.

## P04 ManiSkill observation adapter

Implemented the first lightweight ManiSkill observation adapter:

- added adapter functions that convert `state_dict` and `pointcloud` observations into pg3d
  `Observation`, `RobotState`, and `SimGroundTruth` objects;
- kept ManiSkill/SAPIEN/Gymnasium imports lazy by making the adapter operate on duck-typed
  observation/env objects;
- derived Panda robot masks and `cube`/`goal_site` masks from ManiSkill segmentation ids when a
  live env context is passed;
- added `scripts/save_maniskill_observation.py` to save `summary.json`, `observation.npz`,
  optional MP4 renders, and optional Rerun `.rrd` point-cloud artifacts;
- added the optional `viz` extra for `rerun-sdk==0.22.1`; the initially proposed
  `rerun-sdk==0.31.4` requires NumPy 2 and conflicts with pg3d's current NumPy `<2` constraint;
- documented `state_dict`, point-cloud, segmentation, MP4, and Rerun commands.

Validation:

- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv lock` resolved 238 packages and added
  `pyarrow==24.0.0` plus `rerun-sdk==0.22.1`.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv lock --check` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv sync --extra cu129 --extra maniskill --extra viz --group dev --group notebooks`
  passed after rerunning with network access and installed the optional Rerun dependencies.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make test` passed: 15 tests.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make lint` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make smoke` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make gpu-check` passed outside the sandbox with
  `torch 2.11.0+cu129`, CUDA 12.9, and NVIDIA GeForce RTX 5090.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make maniskill-check` failed inside the sandbox because
  SAPIEN could not see a supported physical device, then passed outside the sandbox.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/save_maniskill_observation.py --obs-mode state_dict --output-dir /tmp/pg3d-ms-state`
  failed inside the sandbox for the same SAPIEN device reason, then passed outside the sandbox.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/save_maniskill_observation.py --obs-mode pointcloud --output-dir /tmp/pg3d-ms-pc`
  passed outside the sandbox with 16,384 points, 1,733 Panda robot-mask points, and 26 cube-mask
  points.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/save_maniskill_observation.py --obs-mode pointcloud --video-frames 3 --output-dir /tmp/pg3d-ms-video`
  passed and wrote an MP4.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/save_maniskill_observation.py --obs-mode pointcloud --rerun-path /tmp/pg3d-ms-pc.rrd --output-dir /tmp/pg3d-ms-rerun`
  passed and wrote a Rerun `.rrd` artifact.

## P05 ManiSkill reach dataset writer

Implemented the first reach dataset scaffold:

- added lazy registration for `PG3DReach-Narrow-v0` and `PG3DReach-Medium-v0`;
- added a Panda custom reach task with `goal_site`, `extra.tcp_pose`, `extra.goal_pos`, and
  TCP-to-goal success signals;
- added pure NumPy/Zarr dataset utilities for workspace point-cloud crop/downsample/pad, 7D
  arm-joint action labels, schema validation, metadata, and git commit capture;
- added `scripts/write_maniskill_reach_dataset.py` for smoke-scale motion-planning demos and
  `scripts/replay_maniskill_reach_dataset.py` for replay sanity checks;
- documented the Zarr schema and commands in the architecture and ManiSkill runbooks.

Validation:

- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv lock --check` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make test` passed: 21 tests.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make lint` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make smoke` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make maniskill-check` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make gpu-check` passed with `torch 2.11.0+cu129`, CUDA 12.9,
  and NVIDIA GeForce RTX 5090.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/write_maniskill_reach_dataset.py --num-demos 3 --output /tmp/pg3d-reach-smoke.zarr --overwrite`
  failed inside the sandbox because SAPIEN could not see a supported physical device, then passed
  outside the sandbox. It wrote 3 successful demos, 62 total steps, 7D `action`, 8D `sim_action`,
  and fixed-size `[62, 512, 3]` point clouds.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/replay_maniskill_reach_dataset.py --dataset /tmp/pg3d-reach-smoke.zarr --episodes 3`
  failed inside the sandbox for the same SAPIEN device reason, then passed outside the sandbox with
  all three episodes successful.
- Dataset sanity: every row had 512 valid cropped points, robot-mask counts ranged from 35 to 46
  points per row, and each episode's final success flag was true.

Follow-up:

- updated `scripts/replay_maniskill_reach_dataset.py` to save optional MP4 videos and one Rerun
  `.rrd` per replayed episode;
- replay Rerun files now log point cloud, robot points, target, and TCP on a `step` timeline so the
  viewer can play the episode instead of showing only a static observation snapshot;
- documented replay visualization commands in `docs/runbooks/commands.md` and
  `docs/runbooks/maniskill_setup.md`.

Validation:

- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make test` passed: 21 tests.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make lint` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make smoke` passed.
- `MPLCONFIGDIR=/tmp/pg3d-mpl UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/write_maniskill_reach_dataset.py --num-demos 3 --output /home/krishna/code/pg3d/artifacts/reach-dataset-smoke/pg3d-reach-smoke.zarr --overwrite`
  passed outside the sandbox and wrote 3 successful demos.
- `MPLCONFIGDIR=/tmp/pg3d-mpl UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/replay_maniskill_reach_dataset.py --dataset /home/krishna/code/pg3d/artifacts/reach-dataset-smoke/pg3d-reach-smoke.zarr --episodes 3 --video-dir /home/krishna/code/pg3d/artifacts/reach-dataset-smoke/videos --rerun-dir /home/krishna/code/pg3d/artifacts/reach-dataset-smoke/rerun`
  passed outside the sandbox and wrote `episode_000` through `episode_002` MP4 and Rerun files.

## README and future-prompt cleanup

- updated `README.md` with current ManiSkill reach dataset status, optional Rerun setup, and
  dataset/replay commands;
- updated P06-P10 prompts to tell future Codex sessions not to presume missing project details and
  to ask clarifying questions when uncertain.

## P06 DP3 reach dataset integration

Implemented the first simulation-free DP3 reach training path:

- added `pg3d.policies.dp3.reach_dataset.ReachSequenceDataset` for fixed-horizon sampling from the
  P05 Zarr schema;
- added fitted per-feature normalizers for `point_cloud`, `agent_pos`, and `action`;
- kept simulator/eval arrays such as target position, TCP pose, success, robot masks, and
  simulator actions out of DP3 policy batches;
- added `scripts/train_dp3_reach.py` for smoke-scale behavior-cloning steps with optional W&B
  scalar/histogram logging;
- added `scripts/eval_dp3_reach_dataset.py` for dataset-only checkpoint inference sanity checks;
- documented the training/eval commands in README and `docs/runbooks/commands.md`.

Validation:

- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv lock --check` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run pytest tests/test_dp3_reach_dataset.py tests/test_dp3_policy_smoke.py`
  passed: 8 tests.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make test` passed: 26 tests.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make lint` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make smoke` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make maniskill-check` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/train_dp3_reach.py --dataset artifacts/reach-dataset-smoke/pg3d-reach-smoke.zarr --device cpu --max-steps 1 --batch-size 2 --checkpoint-out /tmp/pg3d-dp3-reach-smoke.pt`
  passed with `bc_loss=0.699417` and wrote a checkpoint.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/eval_dp3_reach_dataset.py --dataset artifacts/reach-dataset-smoke/pg3d-reach-smoke.zarr --checkpoint /tmp/pg3d-dp3-reach-smoke.pt --device cpu --max-batches 1 --batch-size 2`
  passed with `action_shape=(2, 8, 7)`.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/train_dp3_reach.py --dataset artifacts/reach-dataset-smoke/pg3d-reach-smoke.zarr --device cpu --max-steps 1 --batch-size 2 --wandb-mode offline --log-histograms`
  reached W&B initialization, but W&B could not create `~/.cache/wandb` or local service sockets in
  the sandbox. The trainer now warns and continues unless `--wandb-required` is set.

## P06 DP3 policy rollout visualization

Implemented the first closed-loop policy rollout visualization path:

- added `scripts/rollout_dp3_reach_policy.py` for loading a trained reach checkpoint, creating a
  live `PG3DReach-*` ManiSkill env from dataset metadata, and rolling out DP3 action chunks;
- supports dataset-seed rollouts and fresh-seed rollouts that skip training seeds when a dataset is
  provided;
- maintains the DP3 `n_obs_steps` rolling window, crops/downsamples live point-cloud observations
  with the P05 crop config, and converts 7D absolute/delta Panda arm labels back to simulator
  actions with the gripper held open;
- saves `summary.json`, `metrics.jsonl`, one MP4, and one Rerun `.rrd` timeline per rollout episode;
- keeps ManiSkill/SAPIEN/Gymnasium/Rerun imports out of package and helper-module import time;
- documented policy rollout commands in `README.md` and `docs/runbooks/commands.md`.

Validation:

- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv lock --check` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run ruff check scripts/rollout_dp3_reach_policy.py tests/test_dp3_reach_rollout.py`
  passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run pytest tests/test_dp3_reach_rollout.py tests/test_dp3_reach_dataset.py tests/test_dp3_policy_smoke.py`
  passed: 13 tests.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make test` passed: 31 tests.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make lint` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make smoke` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make maniskill-check` passed.
- The first sandboxed policy rollout attempts failed because SAPIEN could not find a supported
  physical device after CPU fallback; rerunning outside the sandbox passed for one dataset-seed
  rollout and one fresh-seed rollout using the existing smoke checkpoint. Both wrote `episode_000.mp4`,
  `episode_000.rrd`, `metrics.jsonl`, and `summary.json`. The short 5-step smoke checkpoint did not
  reach the goal, so those validation commands used `--allow-failure`.

## Reach dataset/training stabilization detour

Implemented the first stability pass before generating non-trivial datasets:

- added `--hold-steps`, default 8, to `scripts/write_maniskill_reach_dataset.py`;
- after first success, the writer now records one action chunk of hold-pose data using current
  Panda arm qpos targets plus open gripper, while preserving hard max-step/truncation limits;
- dataset metadata now records first success step, requested/recorded hold steps, pre-hold distance,
  final distance, minimum distance, and aggregate dataset stats;
- `scripts/train_dp3_reach.py` now defaults to the moderate SimpleDP3 recipe, validation split,
  `pad_after=n_action_steps-1`, AdamW betas `(0.95, 0.999)`, cosine LR warmup, gradient clipping,
  EMA checkpoint state, and richer W&B metrics;
- dataset eval and live rollout scripts prefer EMA checkpoint weights by default when present;
- rollout metrics now include first success step, minimum distance, post-success distance drift, and
  post-success action norms;
- documented the 50-100 episode pilot path, the later 500-episode dataset command, and the moderate
  5090 training recipe in README and `docs/runbooks/commands.md`.

Validation:

- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run ruff check scripts/write_maniskill_reach_dataset.py scripts/train_dp3_reach.py scripts/eval_dp3_reach_dataset.py scripts/rollout_dp3_reach_policy.py tests/test_maniskill_reach_writer.py tests/test_dp3_reach_training.py tests/test_dp3_reach_dataset.py tests/test_dp3_reach_rollout.py`
  passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run pytest tests/test_maniskill_reach_writer.py tests/test_dp3_reach_training.py tests/test_dp3_reach_dataset.py tests/test_dp3_reach_rollout.py`
  passed: 17 tests.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make test` passed: 38 tests.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make lint` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make smoke` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv lock --check` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make maniskill-check` passed.
- `MPLCONFIGDIR=/tmp/pg3d-mpl UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/write_maniskill_reach_dataset.py --num-demos 5 --max-attempts 10 --hold-steps 8 --output /tmp/pg3d-reach-hold-smoke.zarr --overwrite`
  failed inside the sandbox because SAPIEN could not see a supported physical device, then passed
  outside the sandbox with 5/5 successful demos, 140 total steps, and 8 recorded hold steps per
  episode.
- `WANDB_DIR=/tmp/pg3d-wandb WANDB_CACHE_DIR=/tmp/pg3d-wandb-cache WANDB_CONFIG_DIR=/tmp/pg3d-wandb-config UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/train_dp3_reach.py --dataset /tmp/pg3d-reach-hold-smoke.zarr --device cpu --max-steps 25 --batch-size 4 --num-workers 0 --val-ratio 0.2 --val-every 10 --max-val-batches 1 --encoder-output-dim 16 --diffusion-step-embed-dim 32 --down-dims 32 64 --kernel-size 3 --num-inference-steps 2 --wandb-mode offline --log-histograms --histogram-every 20 --checkpoint-out /tmp/pg3d-dp3-hold-smoke.pt`
  passed and wrote an EMA checkpoint; W&B service startup failed in the sandbox and the trainer
  continued without W&B as designed.
- The same offline W&B path passed outside the sandbox for a one-step training smoke and logged
  train/validation metrics to `/tmp/pg3d-wandb`.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/eval_dp3_reach_dataset.py --dataset /tmp/pg3d-reach-hold-smoke.zarr --checkpoint /tmp/pg3d-dp3-hold-smoke.pt --checkpoint-model ema --device cpu --max-batches 1 --batch-size 2 --encoder-output-dim 16 --diffusion-step-embed-dim 32 --down-dims 32 64 --kernel-size 3 --num-inference-steps 2`
  passed with `action_shape=(2, 8, 7)`.
- One dataset-seed and one fresh-seed live rollout using `/tmp/pg3d-dp3-hold-smoke.pt` passed
  outside the sandbox and wrote MP4/Rerun/JSON artifacts under `/tmp/pg3d-policy-hold-rollout-*`.

## Intermediate checkpoints and W&B rollout videos

Implemented directory-based trainer checkpointing and checkpoint-time rollout media:

- replaced the trainer output flag with `--checkpoint-dir`; checkpoint files are named
  `step_XXXXXXXX.pt` for periodic saves and `final_step_XXXXXXXX.pt` for the final save;
- added `--checkpoint-every`, default 5000, while keeping all periodic checkpoints;
- added best-effort W&B checkpoint rollout videos, default enabled only when W&B is active, with
  `--checkpoint-rollout-count`, max-step, post-success, seed-start, and FPS controls;
- reused the live ManiSkill policy rollout helpers for observation adaptation, point-cloud crop,
  action conversion, EMA policy execution, and dataset/fresh seed selection;
- trainer checkpoint rollouts save only local MP4s needed for W&B upload under the checkpoint
  directory; standalone rollout remains the path for Rerun `.rrd` and JSON metrics;
- documented `--no-checkpoint-rollout-videos` for smoke or training environments without
  ManiSkill rendering.

Validation:

- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run ruff check --fix scripts/train_dp3_reach.py scripts/rollout_dp3_reach_policy.py tests/test_dp3_reach_training.py tests/test_dp3_reach_rollout.py`
  passed after fixing one import-order issue.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run ruff check scripts/train_dp3_reach.py scripts/rollout_dp3_reach_policy.py tests/test_dp3_reach_training.py tests/test_dp3_reach_rollout.py`
  passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run pytest tests/test_dp3_reach_training.py tests/test_dp3_reach_rollout.py`
  passed: 15 tests.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv lock --check` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make test` passed: 44 tests.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make lint` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make smoke` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make maniskill-check` passed.
- `git diff --check` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run python scripts/train_dp3_reach.py --dataset /tmp/pg3d-reach-hold-smoke.zarr --device cpu --max-steps 2 --batch-size 4 --num-workers 0 --val-ratio 0.2 --val-every 1 --max-val-batches 1 --encoder-output-dim 16 --diffusion-step-embed-dim 32 --down-dims 32 64 --kernel-size 3 --num-inference-steps 2 --checkpoint-dir /tmp/pg3d-ckpt-dir-smoke --checkpoint-every 1 --no-checkpoint-rollout-videos`
  passed and wrote `step_00000001.pt`, `step_00000002.pt`, and
  `final_step_00000002.pt`.
- The one-step offline W&B checkpoint-video smoke could not initialize the W&B service in the
  sandbox, then passed outside the sandbox. The offline W&B summary logged
  `rollout/video_count=1`, and the trainer wrote
  `/tmp/pg3d-ckpt-video-smoke/rollout_videos/step_00000001/dataset_000.mp4`.

## Focused cleanup pass

Ran the approved no-behavior-change cleanup pass:

- consolidated duplicated JSON-safe serialization in `pg3d.utils.serialization`;
- consolidated tensor-like NumPy/info/frame conversion helpers in `pg3d.utils.arrays`;
- consolidated CLI device selection in `pg3d.utils.devices`;
- moved DP3 reach checkpoint path/save/load helpers into `pg3d.policies.dp3.checkpoint`;
- updated train/eval/rollout/replay/dataset scripts to use the shared helpers while keeping
  ManiSkill/SAPIEN/Gymnasium/Rerun imports lazy;
- left architecture namespace packages in place because milestones and smoke imports rely on
  them;
- left ADR 0007 and older RLBench worklog references as historical migration context;
- refreshed `docs/runbooks/maniskill_setup.md` so the custom reach dataset smoke includes
  `--hold-steps 8`;
- improved `make clean` to remove nested `__pycache__` directories at any package depth.

Validation:

- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run ruff check .` passed.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run pytest tests/test_utils.py tests/test_dp3_reach_training.py tests/test_dp3_reach_rollout.py tests/test_maniskill_reach_dataset.py`
  passed: 25 tests.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache uv run pytest` passed: 48 tests.
- `UV_CACHE_DIR=/tmp/pg3d-uv-cache make smoke` passed.
- `git diff --check` passed.
